# requirements-build.txt — only needed on the machine building the
# Windows .exe, never on a machine just running AutoCatcher from source.
#
# Usage (on Windows, inside the project venv):
#   pip install -r requirements.txt
#   pip install -r requirements-build.txt
#   pyinstaller autocatcher.spec

pyinstaller>=6.0
